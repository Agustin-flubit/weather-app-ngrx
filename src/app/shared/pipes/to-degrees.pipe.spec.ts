import { ToDegreesPipe } from './to-degrees.pipe';

describe('ToDegreesPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDegreesPipe();
    expect(pipe).toBeTruthy();
  });
});
